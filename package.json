{
  "name": "mollier-hx-card",
  "version": "1.0.0",
  "description": "A Lovelace card that renders a Mollier h-x diagram using temperature and humidity sensor data.",
  "source": "src/mollier-hx-card.js",
  "scripts": {
    "build": "parcel build",
    "watch": "parcel watch"
  },
  "targets": {
    "default": {
      "distDir": "dist",
      "publicUrl": "./",
      "includeNodeModules": true
    }
  },
  "devDependencies": {
    "parcel": "^2.8.3"
  },
  "dependencies": {
    "lit": "^3.3.0"
  }
}
